
&После("ПередЗаписью")
Процедура Buro1Goz_ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;	
	КонецЕсли;
	
	Документ = ЭтотОбъект.Отбор.Регистратор.Значение;
	
	Если ТипЗнч(Документ) = Тип("ДокументСсылка.ОприходованиеИзлишковТоваров")
		И Документ.НеДелатьДвиженияПоРезервамТоваровОрганизации Тогда
		
		ЭтотОбъект.Очистить();
			
	КонецЕсли;
	
КонецПроцедуры
